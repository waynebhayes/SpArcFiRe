This directory contains sample scripts which can be used to control
what fv does.  The supported languages/techniques include:

    XPA/C
    XPA/csh     (command-line execution)
    XPA/Tcl
    Tcl         (executed within fv itself)
    AppleScript (executed by itself on MacOS).

